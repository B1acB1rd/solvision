import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SOLANA VISION",
  description: ```Solana Vision is a professional analytics and risk-assessment platform designed to give traders, investors, and builders a complete understanding of activity happening across the Solana blockchain.
It transforms raw on-chain data into clear insights, wallet intelligence, risk signals, and visual analytics that help users make informed decisions with confidence.```,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
